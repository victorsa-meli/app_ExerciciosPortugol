programa {
  funcao inicio() {
    inteiro salario, abono, totals

    escreva("Digite o Salario: ")
    leia(salario)
    escreva("\n Digite o Abono: ")
    leia(abono)


    totals = (salario + abono)

    escreva("O Novo salario com abono é : ", totals)

    
  }
}
